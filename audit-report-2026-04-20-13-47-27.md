# Audit Report — 70-Saas-LevelCert

**Date:** 2026-04-20 13:47:27
**Scope:** project `CLAUDE.md`, `.claude/settings.local.json`, `~/.claude/projects/…/memory/`
**Tool:** `/audit-project` (Tier 1 checks)

---

## CRITICAL (1)

### 1. CLAUDE.md line count 141 > 100
- **Rule:** Check B — CLAUDE.md loads into every session's system prompt; lines > 100 = critical.
- **Evidence:** `wc -l CLAUDE.md` = 141.
- **Fix:** Trim to ≤50 lines. Move duplicative/derivable blocks (full pricing rationale prose, repo-structure ASCII tree, site-routes list, Build Phases long-form, Current State) into `docs/` pointers. Keep only: pricing table (load-bearing), pass-guarantee rule, training-data overrides (Next.js 16 + Vercel), UI design-system pointer, session hygiene, model routing.

---

## WARNING (5)

### 2. CLAUDE.md — AP-1 (kitchen-sink sessions)
- **Rule:** Check D — no `/clear` discipline rule in project CLAUDE.md.
- **Fix:** Add "Start each distinct task with `/clear` to prevent earlier context from polluting later reasoning."

### 3. CLAUDE.md — AP-2 (retry loops)
- **Rule:** Check D — no re-scope-after-failure rule.
- **Fix:** Add "If a Sonnet attempt fails twice on the same task, re-scope or escalate to Opus / `codex exec` — do not retry the same framing."

### 4. CLAUDE.md — AP-4 (marathon session)
- **Rule:** Check D — no handoff pointer in project CLAUDE.md (handoff protocol lives in global `~/.claude/CLAUDE.md` only).
- **Fix:** Add "For session handoffs, follow the wrap-up procedure in `~/.claude/CLAUDE.md` (\"wrap up\" / \"save state\")."

### 5. memory/project_ipas_2026_exam_calendar.md — stale paths, duplicated content
- **Rule:** Check D — stores non-existent `~/Downloads/*.jpg` and wrong repo path (`docs/launch/…` — actual is `docs/_todo/`); full tabular schedule duplicates canonical repo file.
- **Fix (findings-only, in-place, no commit):** Remove Downloads reference; fix path to `docs/_todo/ipas-exam-calendar-2026.md`; trim table to key dates only + pointer to canonical file.

### 6. memory/project_pricing_ladder_and_guarantee.md — duplicates CLAUDE.md
- **Rule:** Check D — "Why this shape" and "Cost impact" blocks repeat content already in CLAUDE.md, paying the token tax twice per session.
- **Fix (findings-only, in-place, no commit):** Keep decision outcomes (table + 6-month rule + "How to apply"); remove duplicative rationale blocks.

---

## INFO (6)

### 7. CLAUDE.md — Check A #1: Sonnet-as-default-for-coding rule not explicit
- Global `~/.claude/CLAUDE.md` may cover; run `/audit-global` to verify.

### 8. CLAUDE.md — Check A #2: escalation after 2 failed Sonnet attempts not explicit
- Global may cover; also fixed by applying WARNING #3 above.

### 9. CLAUDE.md — Check A #3: long-write (>500 lines / synthesis / extraction / reformatting) delegation rule not explicit
- Partial coverage exists ("mechanical, bulk, parallelizable"); fixed by adding explicit threshold.

### 10. .claude/settings.json missing
- File does not exist. Fine if global settings cover project needs.

### 11. .claude/settings.local.json — no top-level `model` or `autoCompactWindow`
- Global may cover; verify via `/audit-global`.

### 12. .claudeignore missing + no project-level `permissions.deny`
- Large dirs (`node_modules/`, `web/.next/`, `social/assets/`, `content/` binary outputs) may bloat open-ended explores.
- **Fix:** Create `.claudeignore` listing those dirs.

---

## Fix plan

| # | File | Action | Commit? |
|---|------|--------|---------|
| 1, 2, 3, 4, 7–9 | `CLAUDE.md` | Rewrite: trim to ≤50 lines, add session-hygiene + explicit model-routing rules | yes |
| 5 | `memory/project_ipas_2026_exam_calendar.md` | Fix stale paths, trim duplicated schedule | no (memory — in-place) |
| 6 | `memory/project_pricing_ladder_and_guarantee.md` | Remove rationale blocks duplicated by CLAUDE.md | no (memory — in-place) |
| 12 | `.claudeignore` | Create | yes |

10 and 11 left as informational — verify with `/audit-global`.
